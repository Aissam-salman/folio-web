import Menu from '@/components/work/Menu';
import React from 'react';

const Page = () => {
    return (
        <main className={`p-20 h-[100vh] bg-white`}>
            <Menu/>
        </main>
    )
        ;
};

export default Page;